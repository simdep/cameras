api_platform:
    # The title of the API.
    title: 'API LAPI'

    # The description of the API.
    description: 'Cette API gère les données de l’application LAPI'

    # The version of the API.
    version: '0.0.1'

    mapping:
        paths: ['%kernel.project_dir%/src/Entity']
